# AGENTS.md

## Package manager

This project uses **npm** (not pnpm or yarn). Always use `npm install` for adding dependencies.

## Island architecture

This project uses Preact for basic interactivity and state management.
